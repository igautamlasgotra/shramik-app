rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public repository example rules. Tighten these further per role if needed.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
