rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Demo rules for the semester project. Tighten these before production.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
